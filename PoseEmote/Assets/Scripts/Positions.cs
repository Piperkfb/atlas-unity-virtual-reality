using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Positions : MonoBehaviour
{
    bool leftx = false;
    bool rightx = false;

    // Start is called before the first frame update
    void Start()
    {
        //find hand tracking for movement
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
